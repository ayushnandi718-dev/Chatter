# Clerk Publishable Key (Must match backend CLERK_PUBLISHABLE_KEY)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# Backend API Base URL (Leave blank in production for same-origin proxy)
VITE_API_URL=http://localhost:3001
